// import { defineConfig } from '@prisma/client/runtime';

// // This file is used by the Prisma CLI (e.g., `npx prisma migrate dev`)
// // to determine the database connection details.
// export default defineConfig({
//   datasources: [
//     {
//       // The name 'db' must match the name in your schema.prisma file.
//       name: 'db', 
//       // Use the 'url' property here to reference the environment variable
//       // for Prisma Migrate and Introspection.
//       url: process.env.DATABASE_URL,
//     },
//   ],
// });


